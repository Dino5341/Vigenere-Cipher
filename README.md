# Vigenere-Cipher
A Vigenere Cipher is a Cipher that uses a keyword and  multiple Caesar Ciphers shifts to encode any text. This program just needs the input of the keyword and the text you want to encode to output the encoded text. It was also coded in Java using the Java Grasp IDE.
